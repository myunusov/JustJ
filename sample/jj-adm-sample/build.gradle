apply plugin: "application"

version = '1.0.1-SNAPSHOT'

mainClassName = "org.maxur.jj.sample.adm.Launcher"

dependencies {
  compile project(':justj-core')
  compile project(':justj-orm')
}

jar {
  manifest.attributes("Main-Class": mainClassName);
}